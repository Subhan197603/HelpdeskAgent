# Database

- `baseline/` receives the supplied immutable PostgreSQL starter package in Task 0.2.
- `migrations/` is reserved for post-baseline Alembic changes.
- `seed/` holds separate reference-data changes.
- `reporting/` holds reporting views and models.
